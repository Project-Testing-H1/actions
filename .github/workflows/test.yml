on:
  push:
  pull_request_review:

jobs:
  call:
    name: "call workflow"
    permissions:
      contents: ${{'write' }}
      
    uses: yellowrook1/project-testing-h1-actions/.github/workflows/workflow_call.yml@main      
    secrets:
      OVER_PAT: ${{github.token || secrets.GITHUB_TOKEN}}


